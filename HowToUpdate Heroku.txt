command line in VSCode, navigate to : 
(venv) PS C:\Users\Dan\Documents\Python\UltraDBSite>
(Make sure to venv/Scripts/activate)

>git add .
>git commit -m "Some message"
>git push heroku master


IF any new packages have been added, run the following before the above.

>pip freeze > requirements.txt    # Updates the requirements.txt file